# EmbeddedPlayground

这是一个 micropython 的每周一例

## git
```
git.exe clone --progress -v "https://github.com/idcomcn2003/EmbeddedPlayground.git" "G:\projects\micropython\EmbeddedPlayground"
git config --global http.sslVerify false 
git pull --tags origin main
```

## install esptool
```
pip3 install esptool
esptool v5.2.0
```


